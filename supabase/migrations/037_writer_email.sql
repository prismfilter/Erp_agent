-- ============================================================================
-- 작가 마스터 이메일 컬럼 추가 (037)
-- 기본정보에 이메일 저장. [id]@[도메인] 형식(도메인은 프리셋 선택 또는 직접입력).
-- ============================================================================

ALTER TABLE writers ADD COLUMN IF NOT EXISTS email TEXT;

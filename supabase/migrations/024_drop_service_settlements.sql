-- ============================================================================
-- 용역 정산 일회성 전환 — service_settlements 테이블 제거 (024)
-- 용역 정산서를 DB에 영속하지 않고 계산 즉시 화면에 표시·PDF/엑셀로만 보관하도록
-- 변경하면서, 더 이상 사용하지 않는 service_settlements 테이블을 삭제한다.
-- forward-only: 기존 014 마이그레이션은 수정하지 않고 신규 마이그레이션으로 제거.
-- (invoices.paid_at 컬럼은 정산 계산에 계속 사용하므로 유지)
-- ============================================================================

DROP TABLE IF EXISTS public.service_settlements CASCADE;

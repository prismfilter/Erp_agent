-- ============================================================================
-- 035: 작가 플레이리스트 URL (writers.playlist_urls)
-- ----------------------------------------------------------------------------
-- 작가 상세 기본정보에 스포티파이 플레이리스트 등 URL을 최대 3개까지 저장한다.
-- 빈 배열 = 미등록. (개수 제한은 애플리케이션/스키마에서 최대 3개로 강제)
-- ============================================================================

ALTER TABLE public.writers
  ADD COLUMN IF NOT EXISTS playlist_urls TEXT[] NOT NULL DEFAULT '{}';

COMMENT ON COLUMN public.writers.playlist_urls IS '작가 플레이리스트 URL(최대 3개). 빈 배열=미등록';
